let copy = 'copy';
let named = 123;
function 함수(x) {
    // x(파라미터는 숫자가 들어와야 하고) return에는 숫자가 들어와야 한다.
    return x * 2;
}
let shim = { name: 'kid', age: '123' };
class User {
    constructor() {
    }
}
